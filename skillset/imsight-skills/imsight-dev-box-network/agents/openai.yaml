interface:
  display_name: "Imsight Dev Box Network"
  short_description: "Set up dev box networking and tunnels"
  default_prompt: "Use $imsight-dev-box-network use help to see Imsight networking, proxy, and tunnel subcommands."
policy:
  allow_implicit_invocation: false
