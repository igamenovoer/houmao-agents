interface:
  display_name: "imsight-dev-box-network"
  short_description: "Set up dev-box networking and remote access"
  default_prompt: "Use $imsight-dev-box-network use help to see Imsight networking, proxy, tunnel, and remote browser subcommands."
policy:
  allow_implicit_invocation: false
