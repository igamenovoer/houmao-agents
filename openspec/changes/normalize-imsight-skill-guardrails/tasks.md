## 1. Authoring Contract

- [x] 1.1 Synchronize the suite and bundled style guides with the negative-only Guardrail Authoring contract and a compliant example.
- [x] 1.2 Update `imsight-agent-skill-handling` create, format, report-template, entrypoint, and migration guidance to separate prohibitions from positive operations.

## 2. Runtime Skill Migration

- [x] 2.1 Normalize `imsight-autodev-master` guardrails and remove procedural positive tails from its layered pages.
- [x] 2.2 Normalize `imsight-autodev-slave` entrypoint and command guardrails while preserving request, workspace, lifecycle, and failure behavior in substantive content.
- [x] 2.3 Normalize `imsight-dev-box-network` and `imsight-info-gathering` guardrails, strengthening substantive LibGen connectivity-test guidance where needed.
- [x] 2.4 Normalize `imsight-llm-wiki` runtime guardrails and remove disguised operation steps without editing `org/src/`.
- [x] 2.5 Normalize `imsight-project-automation` guardrails while preserving evidence, baseline, scope, stop, and reporting behavior.
- [x] 2.6 Normalize `imsight-project-explore`, `imsight-project-mgr`, and `imsight-project-misc` guardrails.
- [x] 2.7 Add a concise negative-only Guardrails section to `imsight-project-design` without duplicating its workflow.

## 3. Validation

- [x] 3.1 Verify active runtime Guardrails contain no uppercase `MUST`, no `DO NOT forget ...`, and every Imsight entrypoint has Guardrails.
- [x] 3.2 Run the available skill validator for all Imsight skill entrypoints and inspect actual Guardrails blocks for negative-only form.
- [x] 3.3 Validate the OpenSpec change, check the Git diff, and confirm immutable `imsight-llm-wiki/org/src/` provenance is untouched.
