interface:
  display_name: "Imsight Autodev Master"
  short_description: "Layered dispatch to Houmao slave agents"
  default_prompt: "Use $imsight-autodev-master to choose a master workflow or invocation leaf, inspect a Houmao-managed slave, and dispatch an OpenSpec request."
policy:
  allow_implicit_invocation: false
